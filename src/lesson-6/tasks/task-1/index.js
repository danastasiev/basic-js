/*
Create a function that returns a christmas tree of the correct height.

For example:

height = 5 should return:

    *
   ***
  *****
 *******
*********

Height passed is always an integer between 0 and 100.

Use \n for newlines between each line.

Pad with spaces so each line is the same length. The last line having only stars, no spaces.
 */

function christmasTree(height) {
}

// JSON.stringify(christmasTree(0), "\"\""
// JSON.stringify(christmasTree(1), "\"*\""
// JSON.stringify(christmasTree(2), "\" * \\n***\""
// JSON.stringify(christmasTree(3), "\"  *  \\n *** \\n*****\""
// JSON.stringify(christmasTree(4), "\"   *   \\n  ***  \\n ***** \\n*******\""
// JSON.stringify(christmasTree(10), "\"         *         \\n        ***        \\n       *****       \\n      *******      \\n     *********     \\n    ***********    \\n   *************   \\n  ***************  \\n ***************** \\n*******************\""